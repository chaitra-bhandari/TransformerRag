[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transformer-spec-rag"
version = "1.0.0"
description = "AI-powered document extraction"
